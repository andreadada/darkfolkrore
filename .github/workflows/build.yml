name: Build

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle cache
        uses: gradle/actions/setup-gradle@v4

      - name: Build, test, validate resources, and audit release JAR
        run: ./gradlew clean build --no-daemon --no-configuration-cache --stacktrace

      - name: Upload production JAR
        uses: actions/upload-artifact@v4
        with:
          name: darkfolklore-core-0.2.0
          path: build/libs/darkfolklore-core-0.2.0.jar
          if-no-files-found: error
          retention-days: 14
