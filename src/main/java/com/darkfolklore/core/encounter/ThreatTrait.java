package com.darkfolklore.core.encounter;

/** DarkFolklore-owned baseline traits. Optional providers may mirror them, but these guarantees never depend on a provider. */
public enum ThreatTrait {
    TANK,
    BRUTAL,
    SWIFT
}
